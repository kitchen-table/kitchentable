{
  "name": "@kitchentable/shell",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri shell: window, tray, updater, and the IPC<->socket proxy.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  }
}
