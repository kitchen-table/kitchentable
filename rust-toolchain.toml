[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Universal-binary targets are added by release.yml, not here: listing them
# makes every Linux CI run download two Apple std sets it never uses.
